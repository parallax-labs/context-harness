+++
title = "Documentation"
description = "Everything you need to install, configure, and deploy Context Harness."
sort_by = "weight"
template = "docs/section.html"
page_template = "docs/page.html"
+++

Context Harness is a local-first context indexing framework that ingests documentation and code from any source, stores it in SQLite, and exposes it to AI tools via CLI and MCP-compatible HTTP server. One binary. Zero cloud dependencies.

